<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/blue"
    android:padding="32dp">

    <!-- Tiêu đề -->

    <!-- Mô tả -->

    <TextView
        android:id="@+id/textTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="35dp"
        android:text="Trợ năng đọc văn bản"
        android:textColor="#0051ff"
        android:textSize="35dp"
        android:textStyle="bold" />

    <!-- Nút kích hoạt -->


    <!-- Nút tắt -->
    <TextView
        android:id="@+id/textDescription"
        android:layout_width="330dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="40dp"
        android:gravity="center"
        android:text="Bật tính năng đọc văn bản để ứng dụng tự động đọc nội dung hiển thị hoặc phần đang được chọn trên màn hình."
        android:textColor="#424242"
        android:textSize="20dp" />

    <Button
        android:id="@+id/buttonActivateTTS"
        android:layout_width="334dp"
        android:layout_height="155dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="#4CAF50"
        android:paddingLeft="32dp"
        android:paddingTop="16dp"
        android:paddingRight="32dp"
        android:paddingBottom="16dp"
        android:text="BẬT TÍNH NĂNG ĐỌC VĂN BẢN"
        android:textAllCaps="true"
        android:textColor="#FFFFFF"
        android:textSize="25dp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/buttonDeactivateTTS"
        android:layout_width="341dp"
        android:layout_height="167dp"
        android:backgroundTint="#E53935"
        android:paddingLeft="32dp"
        android:paddingTop="16dp"
        android:paddingRight="32dp"
        android:paddingBottom="16dp"
        android:text="TẮT TÍNH NĂNG"
        android:textAllCaps="true"
        android:textColor="#FFFFFF"
        android:textSize="25dp"
        android:textStyle="bold" />

</LinearLayout>
